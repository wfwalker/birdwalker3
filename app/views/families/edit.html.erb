<div class="lead"><%= @page_title %></div>

<div class="pagecontentmodule">
	<div class="pagecontentitem">
		<%= form_for @family, :url => family_path(@family), :html => { :class => 'form-horizontal' }  do |form| %>
		  <%= render :partial => 'form' %>
		  <%= submit_tag 'Save' %>
		<% end %>
	</div>
</div>
