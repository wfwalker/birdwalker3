<div class='container'>
	<h3><%= @page_title %></h3>

	<%= form_for @sighting, :html => { :class => 'form-horizontal' }  do |form| %>
		<%= render :partial => 'form' %>
		<%= submit_tag 'Save' %>
	<% end %>
</div>