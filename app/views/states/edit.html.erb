<div class="lead">
	<%= @page_title %>
	<span style="font-size: 9pt"><%= link_to 'show', state_url(@state) %></span>
</div>

<div class="pagecontentmodule">
	<div class="pagecontentitem">
		<%= form_for @state, :url => state_path(@state), :html => { :class => 'form-horizontal' } do |form| %>
		  <%= render :partial => 'form' %>
		  <%= submit_tag 'Save' %>
		<% end %>
	</div>
</div>

