<div id="pagebody">
	<%= render :partial => 'page_header', :object => @state %>

	<div id="pagecontent">
		<div class="pagecontentmodule">
			<div class="moduletitle"><%= @state.species.size %> Species
			</div>
			<%= render :partial => 'sightings/species_by_month_table', :object => @map, :locals => { :totals => @totals } %>
		</div>
	</div>
</div>
