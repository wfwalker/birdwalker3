<%= render :partial => 'page_header', :object => @species %>

<div class="pagecontentmodule">
	<div class="moduletitle">
		<%= @species.locations.size %> Locations
	</div>
	<%= render :partial => 'sightings/locations_by_month_table', :object => @map, :locals => { :totals => @totals }%>
</div>
