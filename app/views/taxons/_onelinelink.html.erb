<% taxon = onelinelink %>

<% logger.error(taxon.common_name) %>

<div id="<%= taxon_anchor_name(taxon) %>">
	<% link_title = "Species report, " + taxon.common_name %>
	<%= link_to taxon.common_name, taxon_latin_name_path(taxon), :title => link_title %>
</div>
