<% taxons_list = divided_list %>

<% taxons_by_family = Taxon.map_by_family(taxons_list) %>

<!-- TODO: not sorted!  -->
<% sorted_families = taxons_by_family.keys %>

<% if (show_headings) %>
	<h4><%= taxons_list.size %> taxons</h4>
<% end %>

<% for family in sorted_families %>
	<h5><%= family %></h5>     
	<!-- TODO: not sorted!  -->
	<% sorted_taxons_list = taxons_by_family[family] %>	    
	<%= render :partial => 'taxons/onelinelink', :collection => sorted_taxons_list %>
<% end %>
