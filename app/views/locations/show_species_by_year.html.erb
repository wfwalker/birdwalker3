<%= render :partial => 'page_header', :object => @location %>

<div class="pagecontentmodule">
	<h4><%= @location.taxons.size %> Species</h4>
	<%= render :partial => 'sightings/species_by_year_table', :object => @map, :locals => { :totals => @totals }%>
</div>

