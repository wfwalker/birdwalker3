<% county_list = undivided_list %>

<div class="pagecontentmodule">
	<% if (show_headings) %>
		<div class="moduletitle"><%= county_list.size %> Count<%= county_list.size > 1? 'ies' : 'y' %></div>
	<% end %>
	
	<%= render :partial => 'counties/onelinelink' , :collection => county_list, :locals => { :show_extras => true } %>
</div>