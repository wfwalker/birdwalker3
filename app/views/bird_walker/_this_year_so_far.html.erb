<div class="pagecontentmodule">
	<h4>This year so far</h4>
	<div class="pagecontentitem">
		<p>So far in
		<%= link_to 'day ' + Date.today.yday.to_s + ' of ' + Date.today.year.to_s + ' we have seen ' + @this_year_species.size.to_s + ' species', :controller => 'taxons', :action => 'year_list', :id => Date.today.year %>.
		Last year at this time we had seen <%= @last_year_species.size.to_s %>.</p>
	</div>
</div>
