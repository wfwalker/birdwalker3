<div id="pagebody">
	<div id="pageheader">
		<div id="pagetitle"><%= @page_title %>
	</div>
	
	<div id="pagecontent">
		<% @trips.each do | trip | %>
			<div>
				<%= link_to trip.name, :controller => 'trip', :action => 'show', :id => trip.id %>
			</div>
		<% end %>
	</div>
</div>