<h3><%= @page_title %></h3>

<div class="pagecontentmodule">
	<div class="pagecontentitem">
		<%= form_for @trip, :html => { :class => 'form-horizontal' }  do |form| %>
  			<%= render :partial => 'form' %>
  			<%= submit_tag "Create" %>
		<% end %>
	</div>
</div>
